if ! command -v gitleaks >/dev/null 2>&1; then
  echo "gitleaks が見つかりません。" >&2
  exit 1
fi
gitleaks protect --staged --redact

npm run lint
npm run typecheck
npm run format:check
npm test
npm run build
git add dist
