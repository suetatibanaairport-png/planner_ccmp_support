# CLAUDE.md

> キーワード：しなければならない/してはならない = 必須、すべきである/すべきでない =　実施しない明確な理由がない限り推奨、してもよい =　任意。

このファイルは、このリポジトリで作業する Claude Code (claude.ai/code) 向けのガイドです。

## 概要
Microsoft 365 Planner の CSV エクスポートからアローダイアグラム（AOA/PERT図）を作成し、CCPM 向けの合流バッファ候補・クリティカルチェーンを算出する、完全オフラインの単一 HTML ファイルツール。バックエンドなし、実行時のネットワーク通信なし — 既定の休日カレンダーを含め、すべてビルド時に1つの `dist/Planner_ccmp_support.html` にバンドルされる。

## ドキュメント
### ドキュメント・コードの関係性
ドキュメント・コード・コンフィグなどのは優先度は下記の階層の通りとする。同一階層内では上側に記載されているものの方が優先度が高いものとする。

優先度が相対的に低いものと高いものの内容が相違している場合、優先度が相対的に低いものの内容を修正すべきである。ただし、品質の改善につながる場合はユーザーに対し優先度の高いものの修正を提案しなければならない。

- `docs/開発ガイド.md` プロジェクトの方針。
  - `docs/要件定義書.md`：要件定義をまとめた文章。
    - `docs/機能仕様書.md`：要件定義から機能仕様を設計しその結果を記載したもの。
    - `docs/UI・UX仕様書.md` ：`機能仕様書.md`からUI・UXに関する具体的な仕様を切り出したもの。機能仕様書の一部である。
    - `docs/セキュリティ仕様書.md` ：`機能仕様書.md`からセキュリティに関する具体的な仕様を切り出したもの。機能仕様書の一部である。
    - `docs/エラー仕様書.md` ：`機能仕様書.md`からエラーに関する具体的な仕様を切り出したもの。機能仕様書の一部である。
    - `docs/テスト仕様書.md` ：`機能仕様書.md`からテストに関する具体的な仕様を切り出したもの。機能仕様書の一部である。
      - コード・コンフィグ・テストデータ類。
      - `docs/詳細設計書.md`：機能仕様から具体的な設計をおこないその結果を記載したもの。
      - `README.md`:リポジトリを確認する人間に対する情報提供文書。他のドキュメント・コード・コンフィグから内容を収集する。
  - `LICENSE`
- `CLAUDE.md`

### ドキュメント間の参照の記載
- 優先度が上位のものから下位ののもに対する参照は記載すべきでない。

### ドキュメント更新
タスク（プルリクエストのマージ、コマンドの完了など）を完了したら、ドキュメント類の更新が必要かを実際にドキュメントを参照しない範囲で確認すべきである。更新が必要な場合は更新を提案しなければならない。

## 一般
- 日本語の使用をすべきである。
- 今日の日付が必要となった際は、`date`コマンドを実行しなければならない。モデル内部の知識によってはならない。
- 日時の形式は`YYYY-MM-DD`を使用しなければならない。外部からの入力が異なるフォーマットの際は変換して使用すべきである。
- lint / 単体テスト / フォーマッタは使用しなければならない。

## 変更範囲のルール
- 明示的に要求された変更から逸脱せず実施しなければならない。明示的に要求されていない機能、ツール、パッケージ、コンテンツは追加してはならない。
- 何か追加が必要な際は、まずユーザーに確認しなければならない。
- 明示的に指示がない限りPRコメント、コミットメッセージ、ドキュメントは簡潔に保たなければならない。

## gitオプション
- コミット、プッシュ、その他のgit操作はユーザーの明示的な許可なしに実行してはならない。
- `git push --force`は使用してはならない。
- 読み取り専用の操作(`git status`, `git diff`, `git log`など)は許可なしで実行しても良い。

## コーディングスタイル
### コーディングスタイルの哲学：人間が理解できなければならない
人間の状況判断能​​力と記憶力には限界がある。この点を念頭に置いてコードを書かなければならない。
- コンパクトな機能：人間が一目で完全に理解することが可能な小さく焦点を絞った機能に分割されていなければならない。
- 明確な命名規則：関数名と変数名は、ストーリーを語るものでなければならない。初心者でも流れを理解できる命名でなければならない。
- 最小限の範囲：認知負荷を軽減するために、変数の範囲をできるだけ小さく保たなければならない。
- 読みやすい流れ：コードは物語のように読めるなければならない

### ルール
- 関数は20行以内に収めなければならない。必要に応じて、より小さな関数に分割しなければならない。
- `let`より`const`を優先しなければならない。`var`は使用してはならない。
- ループよりも関数型アプローチ（`forEach`、`map`、`filter`、`reducefor`）を優先しなければならない。
- `async/await` よりも`.then()`を優先しなければならない。
- 明示的な型定義を使用しなければならない。`any`は使用してはならない。
- lint/型エラーを`eslint-disable`、`@ts-ignore`、`@ts-expect-error`で抑制してはならない。代わりに、型定義/根本原因を修正しなければならない。
- マジックナンバーは絶対に使用してはならない。代わりに名前付き定数を使用しなければならない。
- 該当する場合は変数名に単位を含めるべきである。（例：timeout_ms、distance_km）
- DRY原則（Don't Repeat Yourself：繰り返しを避ける）を遵守しなければならない。
- 失敗する可能性のある操作には、必ずtry/catchを追加しなければならない。
- ネットワークリクエスト（フェッチ、API呼び出し）には、`AbortController`を使用したタイムアウト処理を含めなければならない。
- エラーメッセージはコンテキスト（URL、ファイルパスなど）を含む、意味のあるエラーメッセージを提供しなければならない。

### コメント
- デフォルトではコメントは書いてはならない。説明は名前、型、引数構造に任せる。次の行が明らかに何をするかをコメントで繰り返している場合、そのコメントは削除しなければならない。
- コードが何をするのかを決して説明してはならない。適切な名前の関数、変数、型が既にその役割を任せる。もしコメントで理解する必要がある場合は、識別子の名前を変更するか、型を厳密にするか、より小さな関数を抽出する方向に変更すべきである。
- なぜそうするのかが明白でない場合にのみコメントを追加しなければならない。 例えば、隠れた制約、微妙な不変条件、特定のバグに対する回避策、ブラウザやライブラリの癖、読者を驚かせるような動作などです。コメントは将来のメンテナーが見て「これはまだ正しい判断か？」と判断できるようにしなければならない。つまり、ルールだけでなく理由もコメントに含めなければならない。
コメントで現在のタスク、修正、または呼び出し元を参照してはならない。(例えば// see issue #123)そのコンテキストはプルリクエストの説明/コミットメッセージに含めるべき内容である。
- 複数段落のドキュメント文字列や複数行のコメントブロックは記述してはならない。1行の短い記述が上限です。ただし、外部規約（公開パッケージのpublic-API JSDocなど）で絶対的に要求されている場合は、そちらを優先しなければならない。
- リファクタリングを行う際は、 「念のため」に残しておくのではなく、積極的にWHATコメントを削除しなければならない。

## テスト実行
- デフォルトでNode.jsの`node:test`, `node:assert`を使用すべきである。プロジェクトが既に別のランナーを使用している場合は、既存のランナーに従うべきである。
- 外部APIはモック化しなければならない。（テストはAPIキーなしで実行する必要があります）
- `package.json` にテストスクリプトを追加し、test、CI で実行できるようにしなければならない。
 
### ユニットテスト　パターンチェックリスト
ユニットテストをする際は、下記のパターンを網羅してください。
- ハッピーパス (期待されうる通常の入力)
- エッジケース (有効だが通常とは異なる入力)
- コーナーケース (エッジケースに相当する入力の組み合わせ)
- 境界ケース (最大値/最小値, Off-by-one)
- emptyケース (空文字列、空配列、空のオブジェクト)
- Null/undefinedケース
- 無効な入力
- エラーケース (想定されうるエラー、想定されうる例外処理)
- 回帰テスト

###　ゴールデンテスト

###　テストファイルの置き場
- テストコードおよびテストデータはリポジトリルート直下の `test/`に置かなければならない。
- ファイル名：`test_xxx_ts`, `test_data_xxx.yyy`
- リポジトリが巨大な場合は、サブディレクトリに分割すべきである。

### テスト容易性を確保する設計
- 結果の判定と入出力処理は必ず分離しなければならない。ルール（フィルタ、順序付け、上限設定、検証、フォーマット、保持）は、独自のファイル内の純粋関数に記述する。呼び出し元は、ファイルの読み取り、インスタンス生成、ソケット操作、HTTP接続などを担当する。アプリの起動時のみアクセス可能なルールはテストされない。
- 「動作を維持するリファクタリング」だからといって、テストを一切提供しない理由にしてはならない。コードを移動しても、そのコード内のルールについては何も証明されない。少なくとも、移動によって明らかになったルールを抽出し、それらをテストすべきである。
- 「コードに不具合がある場合は新しく追加したテストが必ず失敗すること」を確認しなければならない。一時的に条件を反転させるか、ガードを削除するか、修正をもとに戻して、テストが失敗することを確認してから、一時的な変更をもとに戻すこと。不具合のあるコードに対しても成功するテストはテストしたい事象とは異なる事象をテストしている可能性がある。頻繁に発生するうえ、確認しなければ確実に見過ごされる。
- 大きなファイルを扱う際は、既にファイル内に埋もれている純粋なルールを探し出し、作業の一環として抽出・テストしなければならない。これは新規コードだけでなく、既存コードにも適用しなければならない。「テスト可能」とは、コードベースを積極的に復元するための特性であり、新規行のみを対象とするルールではない。
- 最初にテストする項目を選ぶ際は、失敗がどれだけ静かに発生するかを基準に優先順位を付けるべきである。例外をスローする関数は既にエラーを報告しているが、インデックスのずれ、バイト長と文字長の誤認識、日付の誤り、拡張子の大文字小文字の誤り、寛容なバリデーター、プロトタイプチェーンを読み込むルックアップなど、一見すると間違った値を返す関数は、ユーザーが不正なデータに気づくまで発見できない。こうした関数を優先的にテストすべきである。
- 意図的な非対称性や既知の制限事項は、理由をコメントに添えてテストとして固定するべきである。意図的に異なる2つのほぼ同一のヘルパー（一方はトリミングし、もう一方はしない）、意図的に一部の型をスキップするバリデーター、特定の年以降にのみ有効な日付エポックなど、これらを記録しておかないと、次の読者が「修正」してしまう可能性がある。
- モノレポでは、テストがパッケージ名（ビルド済みパッケージに解決されるdist/）を介してインポートされるのか、ソースパスを介してインポートされるのかを確認しなければならない。パッケージ名を介してインポートされる場合、ソースを編集しても、そのパッケージが再ビルドされるまでは何も変更されません。そのため、ミューテーションチェックは「まだグリーン」と報告し、何も証明されません。

## TypeScriptのベストプラクティス
- 型キャストは使用してはならない。代わりに型ガードを使用しなければならない。
- 独自のユーティリティ関数を作成するのではなく、ライブラリの既存のユーティリティ関数を使用しなければならない。
- 再利用性とテスト容易性を確保するため、純粋なデータ変換関数は必ず個別のファイルに分離しなければならない。
- 説明的なフォーマット名を使用なけれけばならない。
- パッケージを移行またはアップグレードする際は、対象バージョンの正しいAPI署名を必ず確認しなければならない。古いAPIがまだ動作すると思い込んではならない。

##　継続的改善
記憶残す際は、初めに記憶場所を選択しなければならない。
- ルール／ワークフロー／コーディング標準（今後のすべての作業に適用）→このファイル（CLAUDE.md）
- ユーザー、フィードバック/修正、またはプロジェクトのコンテキストに関する情報（コードやgit履歴からは取得できない）→ファイルベースのメモリ（~/.claude/.../memory/、 に1行のポインタを追加MEMORY.md）

このファイルに追加する場合：
1. 追加する前に必ずユーザーに確認しなければならない。
2. 学習内容は適切なセクションに追加しなければならない。（必要に応じて新しいセクションを作成してください）。
3. 記入内容は簡潔かつ実行可能なものでなければならない。

タスク（プルリクエストのマージ、コマンドの完了など）を完了したら、以下の内容がないかセッションを確認すべきである。
- ユーザーがセッション中に修正、リダイレクト、または指示を繰り返した場合、それらがルールの欠落（→ CLAUDE.md）、保持する価値のある事実/設定（→ メモリ）、または新しいスキルの候補を示しているかどうかを評価しなければならない。
- そうであれば、適切な場所に保存することを提案しなければならない。

 ##　自動化の提案
セッション中に同じ指示またはパターンが2回以上与えられた場合：
1. 繰り返し作業を認識し、自動化を提案しなければならない。
2. 最も適切な方法を選択しなければならない。
    - CLAUDE.md：ルール、ガイドライン、ワークフロー（例：リリースプロセス、コーディング標準）
    - スキル/コマンド：パラメータ化可能な実行可能なアクション用（例：/release、/deploy）
    - スクリプト：スクリプト化することでメリットが得られる複雑な複数ステップの操作に使用します。
3. トレードオフを説明し、ユーザーに決定させなければならない。
4. 実装後に期待どおりに動作することを確認しなければならない。

## リポジトリ固有情報

挙動を変更する前に `docs/要件定義書.md`（背景・目的・要求事項の正典）、`docs/機能仕様書.md`（挙動の正典）と `docs/詳細設計書.md`（技術設計）を読むこと。`docs/UI・UX仕様書.md`（画面構成・表示要件）、`docs/セキュリティ仕様書.md`（多層防御・CSP）、`docs/エラー仕様書.md`（エラー/警告コード）はいずれも機能仕様書からの切り出しで、章番号を引き継いでいる。`docs/開発ガイド.md` は開発ツール・ワークフローの方針、`docs/テスト仕様書.md` はテスト計画と `test/data/` フィクスチャ一覧を記載している。いずれも日本語。

### コマンド

```bash
npm run dev              # vite開発サーバー
npm run build             # vite build + postbuild.js（dist/Planner_ccmp_support.html を生成）
npm test                  # vitest run（全テスト。--passWithNoTests は使わない）
npx vitest run test/aoa/buildAoa.test.ts   # 単一テストファイル
npx vitest run -t "循環依存"               # 単一テスト（名前指定）
npm run typecheck         # tsc --noEmit
npm run lint              # eslint .
npm run format             # prettier --write .
npm run format:check      # prettier --check .
```

ツール設定ファイル（`eslint.config.js`／`tsconfig.json`／`vite.config.ts`／`.prettierrc.json`／`.prettierignore`）は `config/` にまとめている。上記の `npm run` スクリプトが内部で `--config`／`-p`／`--ignore-path` 等により参照するため、通常の利用では意識する必要はない。

`git commit` を実行すると、Husky の pre-commit フックが上記すべて（加えて `gitleaks protect --staged`）を実行し、`dist/` を再ビルドしてステージに含める。CI（`main` への push/PR）でも同じチェック（加えて `npm audit --omit=dev --audit-level=high`）が走る。gitleaks はローカルに導入していないとコミットが失敗する（`brew install gitleaks`）。

**`dist/Planner_ccmp_support.html` は `.gitignore` 対象外でリポジトリにコミットする。** 実際の配布物そのもの（利用者はビルドせずダウンロードしてそのまま開く）であるため。ソースとの乖離を作らないこと（pre-commit フックが自動で同期する）。ファイル名は固定名と定めている（リネームの仕組みは詳細設計書.md 1章参照）。

### パイプラインのステージ構成（機能仕様書 4.1）

すべての処理は番号付きステージ `[0]`〜`[14]` を流れる。このステージ分割がアーキテクチャ上の最重要な不変事項。

- **ステージ[0]** — `FileList.length`/`File.size` のみを用いたファイル数・サイズの事前検証。ファイル内容の読み込み**より前**に実行する（`ui/App.ts` の `handleFilesSelected`）。上限超過のファイル群を、内容を一切読み込まずに拒否できる。
- **ステージ[1]** — `FileReader` によるI/O（副作用あり、`ui/App.ts`）。
- **ステージ[2]〜[12]** — CSVパース → 検証 → 正規化 → 依存グラフ構築 → AOA変換 → スケジュール算出（ES/LS/クリティカルパス） → CCPMバッファ検出。**副作用を持たない純粋関数群。DOM・I/Oなし。** ファイルごとに独立して1回実行される（1ファイルの致命的エラーが他のファイルの処理を止めない）。単体テストの対象領域。
- **ステージ[13]〜[14]** — プロジェクト横断の集約、レイアウト、SVG描画（副作用あり、`workspace/`・`layout/`・`render/`・`ui/`）。

`workspace/pipeline.ts`（`processFile`）が1ファイル分のステージ2〜12を統括する。`workspace/Workspace.ts` は読み込み済み全ファイルを横断するステートフルなオーケストレータ（ファイルの追加・解除、ファイル間重複検出 E205/E206、共通時間軸）であり、`ui/` が唯一やり取りしてよいクラス。

### モジュール構成（`src/`）

| モジュール | 責務 |
| --- | --- |
| `csv/` | RFC 4180 準拠のCSVパース。[PapaParse](https://www.papaparse.com/)を`src/csv/parseCsv.ts`でラップする |
| `model/` | CSV行 → `Task` への正規化、`先行タスク:` の抽出 |
| `validate/` | ファイル単位のエラー・警告判定（エラー仕様書のエラーコード） |
| `calendar/` | 営業日計算、タスク/休日それぞれの日付パース（書式が異なる。下記参照） |
| `graph/` | AONグラフ構築、トポロジカルソート、循環検出（E203）、孤立タスク分離 |
| `aoa/` | AON → AOA変換：ダミー矢線の挿入・縮約、イベント番号付け |
| `schedule/` | ES/EF/LS/LF、フロート、クリティカルパス |
| `ccpm/` | 合流バッファ候補の検出 |
| `workspace/` | 複数ファイル・複数プロジェクトの状態管理、ファイル間重複排除、配色パレット、共通時間軸 |
| `layout/` | 複数プロジェクトを縦に積んだ座標計算 |
| `render/` | SVG生成 — テキストは必ず `textContent`、属性は必ず `setAttribute` のみで設定 |
| `ui/` | ファイル入力、ヘッダー・フッター、パネル、ズーム/パン。副作用を持つ唯一のエントリーポイント |
| `security/` | `dom.ts` — 詳細は下記 |

`csv/`〜`ccpm/` はDOMに依存しない純粋関数、`workspace/`・`layout/`・`render/`・`ui/` が副作用層。新しい純粋ロジックは対応するモジュールに置くこと（`ui/App.ts` に書かない）。

`csv/` は自前CSVパーサーから [PapaParse](https://www.papaparse.com/) に移行済み（オフライン要件はビルド時バンドルであれば満たせるため、「独自ユーティリティでなくライブラリを使う」ルールとの矛盾はこちらは解消した）。`layout/` の自前レイアウト計算は、汎用グラフレイアウトライブラリが AOA 座標（ES ベースの絶対値へのピン留め）に対応しないため**現状維持と決定済み**（同ルールの意図的な例外）。開発ガイド.md「技術構成」参照。

### 2種類の日付書式（混同注意）

タスクCSVの日付は `YYYY/MM/DD`（`calendar/parseDate.ts` の `parseTaskDate`）、休日CSVは `YYYY-MM-DD`（`parseHolidayDate`）。書式違反はクラッシュではなく警告（W307）— 日付の欠落・不正時は3営業日の仮置き所要日数にフォールバックし（W304/W305/W306）、ジグザグのエッジとして描画される。

### セキュリティ: DOM書き込みは `security/dom.ts` のみ

`innerHTML`/`outerHTML`/`insertAdjacentHTML`/`document.write` は `security/dom.ts` 以外のすべての箇所で禁止されており、ESLintの `no-restricted-syntax` ルール（`config/eslint.config.js` 参照）で機械的に強制している（慣習ではない）。CSV由来の文字列は必ず `textContent` 経由（`createHtmlElement` の第3引数、または `setText`）で扱う。`setSafeAttribute`／`createHtmlElement` の属性引数は `href`/`xlink:href`/`style`/`on*` を実行時にも拒否する（二次防御）。新規のDOM書き込みコードは必ずこのモジュールを経由すること。

### ビルド: 単一ファイル・オフライン・ハッシュ付きCSP

`vite-plugin-singlefile` がJS/CSSをすべて1つのHTMLにインライン化する（`file://` での動作に必須 — このスキームではESモジュールと`fetch`がブロックされるため、CSV/休日ファイルの読み込みは`fetch`ではなく`FileReader`を使う）。`scripts/postbuild.js` がビルド後に実行され、実際にインライン化された `<script>` のSHA-256を計算してCSPの `<meta>` タグの `script-src` に埋め込み、CSPタグがscriptより前に配置されていなければ例外を投げる（あわせて配布物のファイル名リネームも行う。詳細設計書.md 1章参照）。既定の休日CSVは `main.ts` で `import ... from "...csv?raw"` により埋め込まれる（実行時に取得することはない）。

### テストコード・フィクスチャの配置（`test/`）

テストコード（`test/<module>/*.test.ts`、`src/` の構成を `test/` 配下にミラーする）とテストデータ（`test/data/`）はいずれもリポジトリルート直下の `test/` に置く（`src/` にはテストを置かない）。フィクスチャは静的なCSVで、テストコード内で動的生成はしない。`test/data/minimal/`（AOAパターン1件につき1ファイル — 直列、分岐/合流、交差依存、複数担当、孤立タスク、循環）、`test/data/errors/`（E1xx/E2xx/E4xxの各コードにつき1ファイル、および警告をまとめた1ファイル）、`test/data/planner_tasks_planA_ja.csv` / `planner_tasks_planB_ja.csv`（意図的にタスクIDを分離した、実データ相当の結合テスト用フィクスチャ）。各フィクスチャの用途は `docs/テスト仕様書.md` 3章に一覧がある。

テストファイルの命名は `*.test.ts`（Vitestのデフォルト検出パターン）を用いており、上記「テストファイルの置き場」節にある `test_xxx_ts` という命名規則とは異なる（`.ts` 拡張子を持たないファイルはツールチェーン全体から認識されないため）。この点は要確認・要合意。