# UI・UX仕様書

| 項目 | 内容 |
| --- | --- |
| 版数 | 0.1（たたき台） |
| 作成日 | 2026-08-08 |
| 状態 | ドラフト。**【未定】** と記した箇所は未決定事項。 |
| 関連文書 | [機能仕様書.md](機能仕様書.md)、[詳細設計書.md](詳細設計書.md) |

本書は [機能仕様書.md](機能仕様書.md) 1.4「画面構成」・4.2.4「レイアウト」を切り出したもので、機能仕様書の一部である。章・節番号は機能仕様書での番号をそのまま引き継ぐ（他文書・コード中のコメントに残る「1.4」参照との整合を保つため）。

---

## 1.4 画面構成

1 画面構成。ヘッダー・メイン領域・フッターの 3 区画。**ヘッダー・フッターは 1 行に収まる高さとし、メイン領域を最大限確保する。**

| 区画 | 内容 |
| --- | --- |
| ヘッダー | アプリケーション名（固定表示。読み込んだファイル名は表示しない）と、「ファイルを開く」ボタン（複数選択可。開くたびに読み込み済みの内容に追加する）・「リセット」ボタン（メイン領域をクリアし読み込み状態を初期化する）を 1 行で配置する |
| メイン領域 | アローダイアグラム。左から右へ時間が進む向きで描画。複数プロジェクトは縦方向に積んで同時表示する（機能仕様書 3.5.4 参照）。**ズーム操作用の拡大・縮小ボタンを設ける**（具体的な位置・デザインは **【未定】**） |
| フッター | 左端に「休日設定ファイル」ボタン（機能仕様書 3.7 参照）・「エラー表示」ボタン（エラー仕様書 5.1 参照）・「凡例」ボタン（4.2.4 参照）をこの順で配置する。エラー表示ボタンの右側に、エラー件数を丸バッジで表示する（エラー仕様書 5.1 参照）。右端に著作権表示・ライセンス種別・サードパーティライセンスを横並びで表示（全文は表示しない。機能仕様書 7 章参照） |

- エッジ（作業矢線）は担当者ごとに色分けする
- 配色はダークモード基調とし、純白（#FFFFFF）・純黒（#000000）は使用しない
- キーカラーは `#1414A0`（明度調整は行わず、この値をそのまま使用する）
- **主要な操作（ファイルを開く、リセット、休日設定ファイルの読み込み等）は、操作結果を画面に通知する。** 具体的な通知の表現（表示位置・形式等）は **【未定】**

---

## 4.2.4 レイアウト

座標計算方式（時間軸との対応、交差最小化のアルゴリズム等）は本書の対象外とする。ここでは表示上の要件のみを定める。

| 項目 | 仕様 |
| --- | --- |
| 矢線の描画 | **折れ線。** ただし所要日数が仮置き（機能仕様書 3.6 参照）のタスクは**ジグザグの実線**とする |
| 色分け | 実作業の矢線を担当者ごとに色分けする。**色の割り当ては全プロジェクト横断で一貫させる**（機能仕様書 3.5.3）。**固定のパレットは持たず、読み込み済みの全プロジェクトの担当者数に応じて色相環（Hue）を均等分割し、都度動的に生成する。** ファイルの追加・解除で担当者数が変わるたびに再生成される。**色と担当者名の対応は凡例パネルで確認できる**（下記参照） |
| プロジェクト境界 | 複数プロジェクト表示時、**各プロジェクトの領域をグレーの水平線で区切る**（プロジェクト名は表示しない。機能仕様書 3.5.4 参照） |
| ダミー | 破線・無彩色 |
| クリティカルチェーン | **強調表示は矢線（エッジ）のサイズを大型化することで表現する**（線幅を太くする） |
| 合流バッファ候補 | **強調表示は合流イベント（ノード）のサイズを大型化することで表現する** |
| 操作 | **ズーム・パンに対応する（Google マップの UI と同様のマウスホイール操作・ドラッグ操作）。タスク詳細のツールチップ表示は行わない** |
| プロジェクトを縦積み表示（機能仕様書 3.5.4 案 A）する場合の時間軸 | 横軸は**グローバル営業日番号**（機能仕様書 4.1.5 参照）とする。カレンダー日付そのものへの変換は行わない |

**凡例パネル：** 担当者ごとの色分けが、どの担当者を指すか確認できるよう、凡例を設ける。

- フッター左端の「凡例」ボタン（1.4 参照）で開閉する。パネルは通常は折りたたまれている
- 展開すると、**担当者名と、その担当者に割り当てられた色の組み合わせを一覧表示する**（「未アサイン」用の色を含む。機能仕様書 3.6 参照）
- 色相環による動的な色再生成（上記「色分け」参照）が発生するたびに、凡例の内容も追従して更新される
